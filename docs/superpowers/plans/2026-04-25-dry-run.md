# Dry Run Flag Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add `--dry-run` to `minizen run`, `digest preview`, and `digest send-test` so users can inspect results without making costly LLM calls or sending emails.

**Architecture:** `run_pipeline()` gains a `dry_run` parameter that short-circuits after fetching articles. The `run` command passes it through. The digest commands handle dry-run inline: `preview` prints raw articles, `send-test` calls the LLM and prints the plain-text render but skips SMTP. `send-test` also gains a confirmation prompt before making any LLM call in normal (non-dry-run) mode.

**Tech Stack:** Python 3.14, Typer (`typer.confirm`), pytest + pytest-mock.

---

## File Map

| File | Action | Purpose |
|---|---|---|
| `src/minizen/core/pipeline.py` | **Modify** | Add `dry_run: bool = False`; return early after fetch when True |
| `src/minizen/cli/commands/run.py` | **Modify** | Add `--dry-run` option; pass to `run_pipeline()` |
| `src/minizen/cli/commands/digest.py` | **Modify** | Add `--dry-run` to `preview` and `send_test`; add confirm prompt to `send_test` |
| `tests/core/test_pipeline.py` | **Modify** | Add dry-run test |
| `tests/cli/commands/test_run.py` | **Modify** | Fix existing pipeline assertion; add `--dry-run` test |
| `tests/cli/commands/test_digest.py` | **Modify** | Fix existing send-test tests; add dry-run and confirm tests |

---

### Task 1: Add `dry_run` to `run_pipeline()`

**Files:**
- Modify: `src/minizen/core/pipeline.py`
- Modify: `tests/core/test_pipeline.py`

- [ ] **Step 1: Write the failing test**

Add to `tests/core/test_pipeline.py`:

```python
def test_pipeline_dry_run_skips_llm_email_and_mark(mocker: MockerFixture) -> None:
    # arrange
    articles = [_make_article(1), _make_article(2)]
    mock_rss = MagicMock()
    mock_rss.fetch_unread.return_value = articles
    mock_email = MagicMock()
    mock_agent = MagicMock()
    mocker.patch("minizen.core.pipeline.MinifluxProvider", return_value=mock_rss)
    mocker.patch("minizen.core.pipeline.EmailProvider", return_value=mock_email)
    mocker.patch("minizen.core.pipeline.DigestAgent", return_value=mock_agent)
    settings = _make_settings()

    # act
    run_pipeline(settings=settings, dry_run=True)

    # assert
    mock_rss.fetch_unread.assert_called_once_with()
    mock_agent.run.assert_not_called()
    mock_email.send.assert_not_called()
    mock_rss.mark_as_read.assert_not_called()
```

- [ ] **Step 2: Run to verify it fails**

```bash
uv run pytest tests/core/test_pipeline.py::test_pipeline_dry_run_skips_llm_email_and_mark -v
```

Expected: FAIL — `TypeError: run_pipeline() got an unexpected keyword argument 'dry_run'`

- [ ] **Step 3: Update `src/minizen/core/pipeline.py`**

```python
import logging
from datetime import date

from minizen.ai.agent import DigestAgent
from minizen.config.models import Settings
from minizen.providers.email.smtp import EmailProvider
from minizen.providers.email.template import render_email
from minizen.providers.rss.miniflux import MinifluxProvider

logger = logging.getLogger(__name__)


def run_pipeline(*, settings: Settings, dry_run: bool = False) -> None:
    """Fetch unread articles, generate a digest, email it, then mark articles as read.

    Args:
        settings: Fully loaded application settings (Miniflux, email, AI config).
        dry_run: When ``True``, fetch articles but skip the LLM call, email send,
            and mark-as-read. Logs a summary instead.
    """
    logger.info("Fetching unread articles from Miniflux")
    rss = MinifluxProvider(config=settings.miniflux)
    articles = rss.fetch_unread()
    if not articles:
        logger.info("No unread articles found, nothing to do")
        return

    logger.info("Found %d article(s)", len(articles))

    if dry_run:
        logger.info(
            "Dry run: %d article(s) fetched. LLM call, email, and mark-as-read skipped.",
            len(articles),
        )
        return

    email = EmailProvider(config=settings.email)
    agent = DigestAgent(model=settings.ai.model, top_n=settings.ai.top_n)
    result = agent.run(articles=articles)
    html, plain_text = render_email(result.markdown)
    today = date.today().strftime("%B %-d, %Y")
    logger.info("Sending digest email to %s", settings.email.to_addr)
    email.send(subject=f"Your Daily Zen — {today}", html=html, plain_text=plain_text)
    logger.info("Marked %d article(s) as read", len(result.articles_used))
    rss.mark_as_read(article_ids=result.articles_used)
```

- [ ] **Step 4: Run tests to verify they pass**

```bash
uv run pytest tests/core/test_pipeline.py -v
```

Expected: 4 passed

- [ ] **Step 5: Commit**

```bash
git add src/minizen/core/pipeline.py tests/core/test_pipeline.py
git commit -m "feat: add dry_run parameter to run_pipeline"
```

---

### Task 2: Add `--dry-run` to `run` command

**Files:**
- Modify: `src/minizen/cli/commands/run.py`
- Modify: `tests/cli/commands/test_run.py`

- [ ] **Step 1: Write the failing test and fix the broken existing one**

The existing `test_run_invokes_pipeline` asserts `mock_pipeline.assert_called_once_with(settings=mock_settings)`. After we add `dry_run=False` to the call, that assertion will fail. Update it now, and add the new test.

In `tests/cli/commands/test_run.py`, replace the assertion in `test_run_invokes_pipeline`:

```python
    # assert (updated)
    assert result.exit_code == 0
    mock_load.assert_called_once_with(config_path=config_path)
    mock_pipeline.assert_called_once_with(settings=mock_settings, dry_run=False)
```

Then add this new test at the end of the file:

```python
def test_run_dry_run_flag_passes_dry_run_to_pipeline(
    mocker: MockerFixture, tmp_path: Path
) -> None:
    # arrange
    mock_settings = MagicMock()
    mocker.patch(
        "minizen.cli.commands.run.load_settings", return_value=mock_settings
    )
    mock_pipeline = mocker.patch("minizen.cli.commands.run.run_pipeline")
    config_path = tmp_path / "config.toml"
    config_path.touch()
    runner = CliRunner()

    # act
    result = runner.invoke(app, ["run", "--dry-run", "--config", str(config_path)])

    # assert
    assert result.exit_code == 0
    mock_pipeline.assert_called_once_with(settings=mock_settings, dry_run=True)
```

- [ ] **Step 2: Run to verify they fail**

```bash
uv run pytest tests/cli/commands/test_run.py -v
```

Expected: `test_run_invokes_pipeline` FAIL (wrong call args), `test_run_dry_run_flag_passes_dry_run_to_pipeline` FAIL (no `--dry-run` option yet)

- [ ] **Step 3: Update `src/minizen/cli/commands/run.py`**

```python
from pathlib import Path
from typing import Annotated

import typer

from minizen.config.loader import load_settings
from minizen.core.pipeline import run_pipeline

_DEFAULT_CONFIG = Path.home() / ".config" / "minizen" / "config.toml"

_DRY_RUN_OPTION = Annotated[
    bool,
    typer.Option(
        "--dry-run",
        help="Fetch articles but skip LLM call, email send, and mark-as-read.",
    ),
]


def run(
    config: Annotated[
        Path,
        typer.Option(help="Path to the TOML configuration file.", show_default=True),
    ] = _DEFAULT_CONFIG,
    dry_run: _DRY_RUN_OPTION = False,
) -> None:
    """Run the full digest pipeline: fetch, summarise, and email."""
    try:
        settings = load_settings(config_path=config)
    except FileNotFoundError:
        typer.echo(f"Config file not found: {config}")
        typer.echo("Run `minizen setup` to create one.")
        raise typer.Exit(code=1)
    except KeyError as e:
        typer.echo(f"Error: missing environment variable {e.args[0]}")
        raise typer.Exit(code=1)
    run_pipeline(settings=settings, dry_run=dry_run)
```

- [ ] **Step 4: Run tests to verify they pass**

```bash
uv run pytest tests/cli/commands/test_run.py -v
```

Expected: all tests pass

- [ ] **Step 5: Commit**

```bash
git add src/minizen/cli/commands/run.py tests/cli/commands/test_run.py
git commit -m "feat: add --dry-run flag to run command"
```

---

### Task 3: Add `--dry-run` to `digest preview`

**Files:**
- Modify: `src/minizen/cli/commands/digest.py`
- Modify: `tests/cli/commands/test_digest.py`

- [ ] **Step 1: Write the failing test**

Add to `tests/cli/commands/test_digest.py`:

```python
def test_digest_preview_dry_run_prints_articles_and_skips_llm(
    mocker: MockerFixture,
) -> None:
    # arrange
    mock_settings = _make_settings_mock()
    mocker.patch(
        "minizen.cli.commands.digest.load_settings", return_value=mock_settings
    )
    mock_article = MagicMock()
    mock_article.feed_name = "Tech Feed"
    mock_article.title = "Article Title"
    mock_article.url = "https://example.com/article"
    mock_rss = MagicMock()
    mock_rss.fetch_unread.return_value = [mock_article]
    mocker.patch("minizen.cli.commands.digest.MinifluxProvider", return_value=mock_rss)
    mock_agent = MagicMock()
    mocker.patch("minizen.cli.commands.digest.DigestAgent", return_value=mock_agent)
    runner = CliRunner()

    # act
    result = runner.invoke(app, ["digest", "preview", "--dry-run"])

    # assert
    assert result.exit_code == 0
    assert "Tech Feed" in result.output
    assert "Article Title" in result.output
    assert "https://example.com/article" in result.output
    mock_agent.run.assert_not_called()
```

- [ ] **Step 2: Run to verify it fails**

```bash
uv run pytest tests/cli/commands/test_digest.py::test_digest_preview_dry_run_prints_articles_and_skips_llm -v
```

Expected: FAIL — `No such option: --dry-run`

- [ ] **Step 3: Update `preview` in `src/minizen/cli/commands/digest.py`**

Add `_DRY_RUN_OPTION` type alias after `_VERBOSE_OPTION`:

```python
_DRY_RUN_OPTION = Annotated[
    bool,
    typer.Option(
        "--dry-run",
        help="Fetch articles but skip LLM call and any external sends.",
    ),
]
```

Replace the `preview` function:

```python
@app.command("preview")
def preview(
    config: _CONFIG_OPTION = _DEFAULT_CONFIG,
    verbose: _VERBOSE_OPTION = False,
    dry_run: _DRY_RUN_OPTION = False,
) -> None:
    """Fetch and summarise articles, then print the Markdown digest."""
    configure_logging(verbose=verbose)
    settings = _load(config)
    rss = MinifluxProvider(config=settings.miniflux)
    articles = rss.fetch_unread()
    if not articles:
        typer.echo("No unread articles.")
        return
    if dry_run:
        typer.echo(f"{len(articles)} unread article(s):\n")
        for article in articles:
            typer.echo(f"[{article.feed_name}] {article.title}")
            typer.echo(f"  {article.url}")
        return
    agent = DigestAgent(model=settings.ai.model, top_n=settings.ai.top_n)
    result = agent.run(articles=articles)
    typer.echo(result.markdown)
```

- [ ] **Step 4: Run tests to verify they pass**

```bash
uv run pytest tests/cli/commands/test_digest.py -v
```

Expected: all existing tests plus the new one pass

- [ ] **Step 5: Commit**

```bash
git add src/minizen/cli/commands/digest.py tests/cli/commands/test_digest.py
git commit -m "feat: add --dry-run to digest preview"
```

---

### Task 4: Add confirm prompt and `--dry-run` to `digest send-test`

**Files:**
- Modify: `src/minizen/cli/commands/digest.py`
- Modify: `tests/cli/commands/test_digest.py`

- [ ] **Step 1: Write failing tests and fix existing send-test tests**

The existing send-test tests will break once we add `typer.confirm()` — the test runner has no stdin input, so confirm will abort. Fix them by mocking `typer.confirm`, then add the new tests.

In `tests/cli/commands/test_digest.py`, add `mocker.patch("minizen.cli.commands.digest.typer.confirm")` to the arrange section of these three existing tests:
- `test_digest_send_test_sends_email`
- `test_digest_send_test_exits_early_when_no_articles`
- `test_digest_send_test_verbose_calls_configure_logging`

For example, `test_digest_send_test_sends_email` arrange section becomes:

```python
    # arrange
    mock_settings = _make_settings_mock()
    mocker.patch(
        "minizen.cli.commands.digest.load_settings", return_value=mock_settings
    )
    mocker.patch("minizen.cli.commands.digest.typer.confirm")
    mock_articles = [MagicMock()]
    mock_rss = MagicMock()
    mock_rss.fetch_unread.return_value = mock_articles
    mocker.patch("minizen.cli.commands.digest.MinifluxProvider", return_value=mock_rss)
    mock_result = MagicMock()
    mock_result.markdown = "## Digest"
    mock_agent = MagicMock()
    mock_agent.run.return_value = mock_result
    mocker.patch("minizen.cli.commands.digest.DigestAgent", return_value=mock_agent)
    mock_email = MagicMock()
    mocker.patch("minizen.cli.commands.digest.EmailProvider", return_value=mock_email)
    mocker.patch(
        "minizen.cli.commands.digest.render_email",
        return_value=("<h2>Digest</h2>", "## Digest"),
    )
```

Apply the same `mocker.patch("minizen.cli.commands.digest.typer.confirm")` line to `test_digest_send_test_exits_early_when_no_articles` and `test_digest_send_test_verbose_calls_configure_logging`.

Then add these new tests:

```python
def test_digest_send_test_shows_confirm_before_llm_call(
    mocker: MockerFixture,
) -> None:
    # arrange
    mock_settings = _make_settings_mock()
    mocker.patch(
        "minizen.cli.commands.digest.load_settings", return_value=mock_settings
    )
    mock_rss = MagicMock()
    mock_rss.fetch_unread.return_value = [MagicMock()]
    mocker.patch("minizen.cli.commands.digest.MinifluxProvider", return_value=mock_rss)
    mock_confirm = mocker.patch("minizen.cli.commands.digest.typer.confirm")
    mock_agent = MagicMock()
    mock_agent.run.return_value = MagicMock(markdown="## Digest")
    mocker.patch("minizen.cli.commands.digest.DigestAgent", return_value=mock_agent)
    mocker.patch("minizen.cli.commands.digest.EmailProvider", return_value=MagicMock())
    mocker.patch(
        "minizen.cli.commands.digest.render_email",
        return_value=("<h2>Digest</h2>", "## Digest"),
    )
    runner = CliRunner()

    # act
    runner.invoke(app, ["digest", "send-test"])

    # assert
    mock_confirm.assert_called_once_with(
        "This will make a real LLM API call and send a test email. Continue?",
        abort=True,
    )


def test_digest_send_test_aborts_when_confirm_declined(
    mocker: MockerFixture,
) -> None:
    # arrange
    mock_settings = _make_settings_mock()
    mocker.patch(
        "minizen.cli.commands.digest.load_settings", return_value=mock_settings
    )
    mock_rss = MagicMock()
    mock_rss.fetch_unread.return_value = [MagicMock()]
    mocker.patch("minizen.cli.commands.digest.MinifluxProvider", return_value=mock_rss)
    mocker.patch(
        "minizen.cli.commands.digest.typer.confirm", side_effect=typer.Abort()
    )
    mock_agent = MagicMock()
    mocker.patch("minizen.cli.commands.digest.DigestAgent", return_value=mock_agent)
    runner = CliRunner()

    # act
    runner.invoke(app, ["digest", "send-test"])

    # assert
    mock_agent.run.assert_not_called()


def test_digest_send_test_dry_run_skips_confirm_calls_llm_and_prints_plain_text(
    mocker: MockerFixture,
) -> None:
    # arrange
    mock_settings = _make_settings_mock()
    mocker.patch(
        "minizen.cli.commands.digest.load_settings", return_value=mock_settings
    )
    mock_article = MagicMock()
    mock_rss = MagicMock()
    mock_rss.fetch_unread.return_value = [mock_article]
    mocker.patch("minizen.cli.commands.digest.MinifluxProvider", return_value=mock_rss)
    mock_confirm = mocker.patch("minizen.cli.commands.digest.typer.confirm")
    mock_result = MagicMock()
    mock_result.markdown = "## Digest"
    mock_agent = MagicMock()
    mock_agent.run.return_value = mock_result
    mocker.patch("minizen.cli.commands.digest.DigestAgent", return_value=mock_agent)
    mock_email = MagicMock()
    mocker.patch("minizen.cli.commands.digest.EmailProvider", return_value=mock_email)
    mocker.patch(
        "minizen.cli.commands.digest.render_email",
        return_value=("<h2>Digest</h2>", "Plain text digest"),
    )
    runner = CliRunner()

    # act
    result = runner.invoke(app, ["digest", "send-test", "--dry-run"])

    # assert
    assert result.exit_code == 0
    mock_confirm.assert_not_called()
    mock_agent.run.assert_called_once_with(articles=[mock_article])
    mock_email.send.assert_not_called()
    assert "Dry run — email not sent:" in result.output
    assert "Plain text digest" in result.output
```

Note: `test_digest_send_test_aborts_when_confirm_declined` needs `import typer` at the top of the test file. Add it alongside the existing imports.

- [ ] **Step 2: Run to verify they fail**

```bash
uv run pytest tests/cli/commands/test_digest.py -v
```

Expected: the three fixed existing tests FAIL (confirm not yet added), the three new tests FAIL

- [ ] **Step 3: Update `send_test` in `src/minizen/cli/commands/digest.py`**

Replace the `send_test` function (keep `_DRY_RUN_OPTION` added in Task 3 in place):

```python
@app.command("send-test")
def send_test(
    config: _CONFIG_OPTION = _DEFAULT_CONFIG,
    verbose: _VERBOSE_OPTION = False,
    dry_run: _DRY_RUN_OPTION = False,
) -> None:
    """Send a test digest email without marking articles as read."""
    configure_logging(verbose=verbose)
    settings = _load(config)
    rss = MinifluxProvider(config=settings.miniflux)
    articles = rss.fetch_unread()
    if not articles:
        typer.echo("No unread articles.")
        return
    if not dry_run:
        typer.confirm(
            "This will make a real LLM API call and send a test email. Continue?",
            abort=True,
        )
    agent = DigestAgent(model=settings.ai.model, top_n=settings.ai.top_n)
    result = agent.run(articles=articles)
    html, plain_text = render_email(result.markdown)
    if dry_run:
        typer.echo("Dry run — email not sent:\n")
        typer.echo(plain_text)
        return
    today = date.today().strftime("%B %-d, %Y")
    email = EmailProvider(config=settings.email)
    email.send(
        subject=f"[TEST] Your Daily Zen — {today}",
        html=html,
        plain_text=plain_text,
    )
    typer.echo("Test digest sent.")
```

- [ ] **Step 4: Run all tests to verify everything passes**

```bash
uv run pytest -v
```

Expected: all tests pass

- [ ] **Step 5: Commit**

```bash
git add src/minizen/cli/commands/digest.py tests/cli/commands/test_digest.py
git commit -m "feat: add --dry-run and confirm prompt to digest send-test"
```
