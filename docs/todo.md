# Implementation TODOs

- When no config.toml is found, prompt the user to create one with `minizen setup` command.`
- Let the `minizen setup` command create a default `config.toml` file with the necessary structure and placeholders for the user to fill in.
- In setup also prompt the user for credentials and other necessary information to fill in the config file.
- Write a Getting started guide that walks users through the setup process and how to use the CLI for common tasks.
  - example with gmail email and APP password, and how to set up the config file with those credentials.
- Set the default LLM model to Claude Haiku

- Long-term:
  - Do a research of what the best model is to use with API key and free tier, or a cheap
  - Implement more LLM models provider
